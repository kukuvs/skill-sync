{
  "name": "skill-sync",
  "version": "1.1.0",
  "description": "CLI for syncing shared agent skills from a Bitbucket repository.",
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "bin": {
    "skill-sync": "./dist/src/cli.js"
  },
  "files": [
    "dist/src",
    "README.md",
    "docs"
  ],
  "scripts": {
    "build": "node ./scripts/clean-dist.mjs && tsc -p tsconfig.json",
    "check": "eslint . && prettier --check . && pnpm run build && node ./scripts/run-tests.mjs",
    "clean": "node ./scripts/clean-dist.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "test": "pnpm run build && node ./scripts/run-tests.mjs"
  },
  "keywords": [
    "bitbucket",
    "cli",
    "skills",
    "sync"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18.18"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.12.0",
    "@types/node": "^22.7.5",
    "eslint": "^9.12.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.8.0"
  }
}
